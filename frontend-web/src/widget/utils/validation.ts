/**
 * widget title validation
 * @param title
 */
export const validateTitle = title => {
  const result = false;
  return;
};
/**
 * widget title validation
 * @param title
 */
export const validateSeries = title => {
  const result = false;
  return;
};
