bb5325d861f7e34cf1a68e01884e0399
"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.getEnvironmentOptimizations = exports.getDatabaseBenchmarks = exports.getSupportedDatabaseTypes = exports.getDatabaseSpecificConfig = exports.getOracleConfig = exports.getSnowflakeConfig = exports.getBigQueryConfig = exports.getPostgreSQLConfig = exports.getMySQLConfig = void 0;
/**
 * 환경별 설정
 */
const getEnvironmentMultiplier = () => {
    switch (process.env.NODE_ENV) {
        case 'prod':
            return 1.0;
        case 'dev':
            return 0.7;
        case 'local':
            return 0.3;
        default:
            return 0.5;
    }
};
/**
 * Lambda 메모리 크기 기반 연결 수 계산
 */
const calculateMaxConnections = (baseMax) => {
    const envMultiplier = getEnvironmentMultiplier();
    const memoryMB = parseInt(process.env.AWS_LAMBDA_FUNCTION_MEMORY_SIZE) || 512;
    // 메모리 기반 조정 (512MB = 1.0, 1024MB = 1.5, 3008MB = 3.0)
    const memoryMultiplier = Math.min(memoryMB / 512, 3.0);
    return Math.ceil(baseMax * envMultiplier * memoryMultiplier);
};
/**
 * MySQL/MariaDB 특화 설정
 */
const getMySQLConfig = () => ({
    connectionConfig: {
        client: 'mysql2',
        // @ts-ignore - MySQL specific connection properties
        connection: {
            supportBigNumbers: true,
            bigNumberStrings: true,
            dateStrings: true,
            multipleStatements: false,
            charset: 'utf8mb4',
            timezone: '+09:00',
            connectTimeout: 30000,
        },
        pool: {
            min: 0,
            max: calculateMaxConnections(10),
            createTimeoutMillis: 30000,
            acquireTimeoutMillis: 30000,
            idleTimeoutMillis: 300000,
            reapIntervalMillis: 1000,
            createRetryIntervalMillis: 100,
            propagateCreateError: false,
            afterCreate: (conn, done) => {
                const queries = [
                    'SET SESSION sql_mode="TRADITIONAL"',
                    'SET SESSION time_zone="+09:00"',
                    'SET SESSION autocommit=1',
                    'SET SESSION tx_isolation="READ-COMMITTED"',
                ];
                let completed = 0;
                queries.forEach(query => {
                    conn.query(query, (err) => {
                        completed++;
                        if (completed === queries.length) {
                            done(err, conn);
                        }
                    });
                });
            },
        },
    },
    performanceSettings: {
        batchSize: parseInt(process.env.MYSQL_BATCH_SIZE) || 1000,
        queryTimeout: 30000,
        connectionTimeout: 30000,
        maxConnections: calculateMaxConnections(10),
    },
    features: {
        supportsJson: true,
        supportsArrays: false,
        supportsPartitioning: true,
        supportsClustering: false,
        supportsUpsert: true,
        supportsBulkInsert: true,
    },
    optimizationHints: {
        preferredJoinType: 'nested_loop',
        indexStrategy: 'btree',
        partitionStrategy: 'range',
    },
});
exports.getMySQLConfig = getMySQLConfig;
/**
 * PostgreSQL 특화 설정
 */
const getPostgreSQLConfig = () => ({
    connectionConfig: {
        client: 'pg',
        connection: {
            ssl: process.env.NODE_ENV === 'prod' ? { rejectUnauthorized: false } : false,
            statement_timeout: 30000,
            idle_in_transaction_session_timeout: 60000,
            timezone: 'Asia/Seoul',
            application_name: 'VanillaMeta-BI',
        },
        pool: {
            min: 0,
            max: calculateMaxConnections(20),
            createTimeoutMillis: 30000,
            acquireTimeoutMillis: 30000,
            idleTimeoutMillis: 300000,
            reapIntervalMillis: 1000,
            createRetryIntervalMillis: 100,
            propagateCreateError: false,
            afterCreate: async (conn, done) => {
                try {
                    await conn.query('SET timezone TO "Asia/Seoul"');
                    await conn.query('SET statement_timeout TO 30000');
                    await conn.query('SET idle_in_transaction_session_timeout TO 60000');
                    done(null, conn);
                }
                catch (err) {
                    done(err, conn);
                }
            },
        },
        searchPath: ['public'],
    },
    performanceSettings: {
        batchSize: parseInt(process.env.POSTGRESQL_BATCH_SIZE) || 5000,
        queryTimeout: 30000,
        connectionTimeout: 30000,
        maxConnections: calculateMaxConnections(20),
    },
    features: {
        supportsJson: true,
        supportsArrays: true,
        supportsPartitioning: true,
        supportsClustering: false,
        supportsUpsert: true,
        supportsBulkInsert: true,
    },
    optimizationHints: {
        preferredJoinType: 'hash',
        indexStrategy: 'gin',
        partitionStrategy: 'range',
    },
});
exports.getPostgreSQLConfig = getPostgreSQLConfig;
/**
 * BigQuery 특화 설정
 */
const getBigQueryConfig = () => ({
    connectionConfig: {
        client: require('knex-bigquery'),
        connection: {
            projectId: process.env.BIGQUERY_PROJECT_ID,
            keyFilename: process.env.BIGQUERY_KEY_FILE,
            location: process.env.BIGQUERY_LOCATION || 'US',
            maximumBillingTier: parseInt(process.env.BIGQUERY_MAX_BILLING_TIER) || 1,
            useLegacySql: false,
            useQueryCache: true,
        },
        pool: {
            min: 0,
            max: calculateMaxConnections(5),
            createTimeoutMillis: 60000,
            acquireTimeoutMillis: 60000,
            idleTimeoutMillis: 600000,
            reapIntervalMillis: 30000,
            createRetryIntervalMillis: 1000,
            propagateCreateError: false,
        },
        // BigQuery specific options - need to be applied at query level
    },
    performanceSettings: {
        batchSize: parseInt(process.env.BIGQUERY_BATCH_SIZE) || 1000,
        queryTimeout: 300000,
        connectionTimeout: 60000,
        maxConnections: calculateMaxConnections(5),
    },
    features: {
        supportsJson: true,
        supportsArrays: true,
        supportsPartitioning: true,
        supportsClustering: true,
        supportsUpsert: false,
        supportsBulkInsert: true,
    },
    optimizationHints: {
        preferredJoinType: 'hash',
        indexStrategy: 'auto',
        partitionStrategy: 'range', // 날짜 기반 파티셔닝
    },
});
exports.getBigQueryConfig = getBigQueryConfig;
/**
 * Snowflake 특화 설정
 */
const getSnowflakeConfig = () => ({
    // @ts-ignore - Snowflake specific connection properties
    connectionConfig: {
        client: require('../knex-dialects/snowflake'),
        connection: {
            account: process.env.SNOWFLAKE_ACCOUNT,
            username: process.env.SNOWFLAKE_USER,
            password: process.env.SNOWFLAKE_PASSWORD,
            warehouse: process.env.SNOWFLAKE_WAREHOUSE || 'COMPUTE_WH',
            database: process.env.SNOWFLAKE_DATABASE,
            schema: process.env.SNOWFLAKE_SCHEMA || 'PUBLIC',
            role: process.env.SNOWFLAKE_ROLE || 'PUBLIC',
            application: 'VanillaMeta-BI',
            clientSessionKeepAlive: true,
            clientSessionKeepAliveHeartbeatFrequency: 3600,
            timezone: 'Asia/Seoul',
        },
        pool: {
            min: 0,
            max: calculateMaxConnections(10),
            createTimeoutMillis: 60000,
            acquireTimeoutMillis: 60000,
            idleTimeoutMillis: 600000,
            reapIntervalMillis: 30000,
            createRetryIntervalMillis: 1000,
            propagateCreateError: false,
        },
        // Snowflake specific options - need to be applied at query level
    },
    performanceSettings: {
        batchSize: parseInt(process.env.SNOWFLAKE_BATCH_SIZE) || 10000,
        queryTimeout: 300000,
        connectionTimeout: 60000,
        maxConnections: calculateMaxConnections(10),
    },
    features: {
        supportsJson: true,
        supportsArrays: true,
        supportsPartitioning: true,
        supportsClustering: true,
        supportsUpsert: true,
        supportsBulkInsert: true,
    },
    optimizationHints: {
        preferredJoinType: 'hash',
        indexStrategy: 'auto',
        partitionStrategy: 'auto',
    },
});
exports.getSnowflakeConfig = getSnowflakeConfig;
/**
 * Oracle 특화 설정
 */
const getOracleConfig = () => ({
    connectionConfig: {
        client: 'oracledb',
        // @ts-ignore - Oracle specific connection properties
        connection: {
            connectTimeout: 30000,
        },
        pool: {
            min: 0,
            max: calculateMaxConnections(15),
            createTimeoutMillis: 30000,
            acquireTimeoutMillis: 30000,
            idleTimeoutMillis: 600000,
            reapIntervalMillis: 1000,
            createRetryIntervalMillis: 100,
            propagateCreateError: false,
        },
        // Oracle specific options - need to be applied at query level
    },
    performanceSettings: {
        batchSize: parseInt(process.env.ORACLE_BATCH_SIZE) || 1000,
        queryTimeout: 30000,
        connectionTimeout: 30000,
        maxConnections: calculateMaxConnections(15),
    },
    features: {
        supportsJson: true,
        supportsArrays: false,
        supportsPartitioning: true,
        supportsClustering: false,
        supportsUpsert: true,
        supportsBulkInsert: true,
    },
    optimizationHints: {
        preferredJoinType: 'nested_loop',
        indexStrategy: 'btree',
        partitionStrategy: 'range',
    },
});
exports.getOracleConfig = getOracleConfig;
/**
 * 데이터베이스별 설정 팩토리
 */
const getDatabaseSpecificConfig = (databaseType) => {
    const normalizedType = databaseType === null || databaseType === void 0 ? void 0 : databaseType.toLowerCase().trim();
    switch (normalizedType) {
        case 'mysql':
        case 'mysql2':
        case 'mariadb':
            return (0, exports.getMySQLConfig)();
        case 'pg':
        case 'postgres':
        case 'postgresql':
        case 'cockroachdb':
            return (0, exports.getPostgreSQLConfig)();
        case 'bigquery':
            return (0, exports.getBigQueryConfig)();
        case 'snowflake':
            return (0, exports.getSnowflakeConfig)();
        case 'oracle':
        case 'oracledb':
            return (0, exports.getOracleConfig)();
        default:
            return null;
    }
};
exports.getDatabaseSpecificConfig = getDatabaseSpecificConfig;
/**
 * 모든 지원 데이터베이스 타입 반환
 */
const getSupportedDatabaseTypes = () => [
    'mysql',
    'mysql2',
    'mariadb',
    'pg',
    'postgres',
    'postgresql',
    'cockroachdb',
    'bigquery',
    'snowflake',
    'oracle',
    'oracledb',
];
exports.getSupportedDatabaseTypes = getSupportedDatabaseTypes;
/**
 * 데이터베이스별 성능 벤치마크 정보
 */
const getDatabaseBenchmarks = () => ({
    mysql: {
        averageConnectionTime: 50,
        queriesPerSecond: 5000,
        optimalBatchSize: 1000,
        memoryPerConnection: 2, // MB
    },
    postgresql: {
        averageConnectionTime: 80,
        queriesPerSecond: 8000,
        optimalBatchSize: 5000,
        memoryPerConnection: 3, // MB
    },
    bigquery: {
        averageConnectionTime: 1000,
        queriesPerSecond: 100,
        optimalBatchSize: 1000,
        memoryPerConnection: 1, // API 기반
    },
    snowflake: {
        averageConnectionTime: 800,
        queriesPerSecond: 500,
        optimalBatchSize: 10000,
        memoryPerConnection: 2, // MB
    },
    oracle: {
        averageConnectionTime: 100,
        queriesPerSecond: 4000,
        optimalBatchSize: 1000,
        memoryPerConnection: 4, // MB
    },
});
exports.getDatabaseBenchmarks = getDatabaseBenchmarks;
/**
 * 환경별 최적화 설정
 */
const getEnvironmentOptimizations = () => {
    const env = process.env.NODE_ENV || 'development';
    return ({
        local: {
            poolSize: 0.3,
            queryTimeout: 10000,
            connectionTimeout: 10000,
            batchSize: 0.1,
            enableLogging: true,
            enableProfiling: true,
        },
        dev: {
            poolSize: 0.7,
            queryTimeout: 30000,
            connectionTimeout: 30000,
            batchSize: 0.5,
            enableLogging: true,
            enableProfiling: true,
        },
        prod: {
            poolSize: 1.0,
            queryTimeout: 30000,
            connectionTimeout: 30000,
            batchSize: 1.0,
            enableLogging: false,
            enableProfiling: false,
        },
    }[env] || {
        poolSize: 0.5,
        queryTimeout: 30000,
        connectionTimeout: 30000,
        batchSize: 0.5,
        enableLogging: true,
        enableProfiling: false,
    });
};
exports.getEnvironmentOptimizations = getEnvironmentOptimizations;
