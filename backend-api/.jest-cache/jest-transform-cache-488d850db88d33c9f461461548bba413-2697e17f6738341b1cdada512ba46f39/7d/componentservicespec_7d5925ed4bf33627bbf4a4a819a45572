b524baf7e32d44d65da09263397f6a02
"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const testing_1 = require("@nestjs/testing");
const component_service_1 = require("./component.service");
const component_entity_1 = require("./entities/component.entity");
const test_helpers_1 = require("../../test/test-helpers");
const yn_enum_1 = require("../common/enum/yn.enum");
const hybrid_cache_service_1 = require("../common/optimization/hybrid-cache.service");
describe('ComponentService', () => {
    let service;
    let componentRepository;
    let hybridCacheService;
    beforeEach(async () => {
        const module = await testing_1.Test.createTestingModule({
            providers: [
                component_service_1.ComponentService,
                {
                    provide: (0, test_helpers_1.getRepositoryTokenFor)(component_entity_1.Component),
                    useValue: (0, test_helpers_1.createMockRepository)(),
                },
                {
                    provide: hybrid_cache_service_1.HybridCacheService,
                    useValue: (0, test_helpers_1.createMockService)(['get', 'set', 'del', 'invalidate', 'invalidateByEngine', 'invalidateAll']),
                },
            ],
        }).compile();
        service = module.get(component_service_1.ComponentService);
        componentRepository = module.get((0, test_helpers_1.getRepositoryTokenFor)(component_entity_1.Component));
        hybridCacheService = module.get(hybrid_cache_service_1.HybridCacheService);
        // 캐시 메서드 모킹
        hybridCacheService.get.mockResolvedValue(null);
        hybridCacheService.set.mockResolvedValue(undefined);
        hybridCacheService.invalidateByEngine.mockResolvedValue(undefined);
        hybridCacheService.invalidateAll.mockResolvedValue(undefined);
    });
    it('should be defined', () => {
        expect(service).toBeDefined();
    });
    describe('findAll', () => {
        it('should return all active components', async () => {
            const mockComponents = [
                { id: 1, type: 'chart', title: 'Chart', option: '{"type":"line"}', useYn: yn_enum_1.YesNo.YES },
                { id: 2, type: 'table', title: 'Table', option: '{"type":"basic"}', useYn: yn_enum_1.YesNo.YES },
            ];
            componentRepository.createQueryBuilder.mockReturnValue({
                select: jest.fn().mockReturnThis(),
                where: jest.fn().mockReturnThis(),
                getRawMany: jest.fn().mockResolvedValue(mockComponents),
            });
            const result = await service.findAll();
            expect(result).toHaveLength(2);
            expect(result[0].option).toEqual({ type: 'line' });
            expect(result[1].option).toEqual({ type: 'basic' });
        });
    });
    describe('findOne', () => {
        it('should return a component by id', async () => {
            const mockComponent = { id: 1, type: 'chart', title: 'Chart' };
            componentRepository.findOne.mockResolvedValue(mockComponent);
            const result = await service.findOne(1);
            expect(result).toEqual(mockComponent);
            expect(componentRepository.findOne).toHaveBeenCalledWith({ where: { id: 1 } });
        });
    });
    describe('create', () => {
        it('should create a new component when type does not exist', async () => {
            const createDto = {
                type: 'newchart',
                title: 'New Chart',
                category: 'chart',
                option: '{"type":"bar"}',
                description: '',
                icon: '',
                seq: 0,
                useYn: 'Y',
            };
            componentRepository.findOne.mockResolvedValue(null);
            componentRepository.save.mockResolvedValue(Object.assign({ id: 1 }, createDto));
            const result = await service.create(createDto);
            expect(componentRepository.save).toHaveBeenCalled();
            expect(result).toEqual(Object.assign({ id: 1 }, createDto));
        });
        it('should return error message when component type already exists', async () => {
            const createDto = {
                type: 'chart',
                title: 'Chart',
                category: 'chart',
                option: '{"type":"line"}',
                description: '',
                icon: '',
                seq: 0,
                useYn: 'Y',
            };
            componentRepository.findOne.mockResolvedValue({ id: 1, type: 'chart' });
            const result = await service.create(createDto);
            expect(result).toBe('exist same widget');
        });
    });
    describe('remove', () => {
        it('should remove component by id', async () => {
            componentRepository.delete.mockResolvedValue({ affected: 1 });
            const result = await service.remove(1);
            expect(componentRepository.delete).toHaveBeenCalledWith({ id: 1 });
            expect(result).toBe('This action removes a #1 component');
        });
    });
});
