# T03_S12: 프론트엔드 타입 정의 개선

## 태스크 정보
- **스프린트**: S12 - TypeScript 강화 및 코드 품질
- **태스크 ID**: T03_S12
- **우선순위**: 높음
- **예상 소요시간**: 2-3일
- **담당자**: 프론트엔드 개발팀
- **의존성**: T01 완료 후 진행

## 목표
React 애플리케이션의 컴포넌트, API 통신, 상태 관리에서 타입 안전성을 보장하고, 개발자 경험을 향상시킵니다.

## 작업 내용

### 1. API 응답 타입 자동 생성 도구 도입

#### 1.1 OpenAPI 기반 타입 생성
**도구**: `@openapitools/openapi-generator-cli` 또는 `openapi-typescript`

**설치 및 설정**:
```bash
npm install -D openapi-typescript
npm install -D @openapitools/openapi-generator-cli
```

**package.json 스크립트 추가**:
```json
{
  "scripts": {
    "generate-types": "openapi-typescript http://localhost:3000/api-docs -o src/types/api.ts",
    "generate-api-client": "openapi-generator-cli generate -i http://localhost:3000/api-docs -g typescript-axios -o src/api/generated"
  }
}
```

#### 1.2 생성된 API 타입 활용
**파일**: `/workspace/vanillameta/frontend-web/src/types/api.ts`

```typescript
// 자동 생성된 타입 예시
export interface User {
  id: number;
  name: string;
  email: string;
  status: 'active' | 'inactive' | 'suspended';
  createdAt: string;
  updatedAt: string;
}

export interface Dashboard {
  id: number;
  title: string;
  description?: string;
  visibility: 'public' | 'private' | 'shared';
  userId: number;
  createdAt: string;
  updatedAt: string;
}

export interface PaginatedResponse<T> {
  data: T[];
  total: number;
  page: number;
  limit: number;
  totalPages: number;
}
```

#### 1.3 API 클라이언트 타입 래퍼
**파일**: `/workspace/vanillameta/frontend-web/src/api/client.ts`

```typescript
import axios, { AxiosResponse } from 'axios';
import { User, Dashboard, PaginatedResponse } from '../types/api';

export interface ApiResponse<T> {
  success: boolean;
  data: T;
  message?: string;
  errors?: Record<string, string[]>;
}

export class ApiClient {
  private readonly baseURL: string;
  private readonly timeout: number = 10000;

  constructor(baseURL: string) {
    this.baseURL = baseURL;
  }

  private async request<T>(
    method: 'GET' | 'POST' | 'PUT' | 'DELETE',
    endpoint: string,
    data?: any
  ): Promise<ApiResponse<T>> {
    try {
      const response: AxiosResponse<ApiResponse<T>> = await axios({
        method,
        url: `${this.baseURL}${endpoint}`,
        data,
        timeout: this.timeout,
        headers: {
          'Content-Type': 'application/json',
        },
      });

      return response.data;
    } catch (error) {
      throw this.handleError(error);
    }
  }

  // User API
  async getUsers(page: number = 1, limit: number = 10): Promise<ApiResponse<PaginatedResponse<User>>> {
    return this.request<PaginatedResponse<User>>('GET', `/users?page=${page}&limit=${limit}`);
  }

  async getUser(id: number): Promise<ApiResponse<User>> {
    return this.request<User>('GET', `/users/${id}`);
  }

  async createUser(userData: Omit<User, 'id' | 'createdAt' | 'updatedAt'>): Promise<ApiResponse<User>> {
    return this.request<User>('POST', '/users', userData);
  }

  // Dashboard API
  async getDashboards(): Promise<ApiResponse<Dashboard[]>> {
    return this.request<Dashboard[]>('GET', '/dashboards');
  }

  private handleError(error: any): Error {
    if (error.response?.data) {
      return new Error(error.response.data.message || 'API Error');
    }
    return new Error(error.message || 'Network Error');
  }
}
```

### 2. 컴포넌트 Props 타입 정의

#### 2.1 기본 컴포넌트 Props 인터페이스
**파일**: `/workspace/vanillameta/frontend-web/src/types/components.ts`

```typescript
import { ReactNode, HTMLAttributes } from 'react';

// 기본 공통 Props
export interface BaseComponentProps {
  className?: string;
  testId?: string;
  children?: ReactNode;
}

// 버튼 컴포넌트 Props
export interface ButtonProps extends BaseComponentProps, Omit<HTMLAttributes<HTMLButtonElement>, 'onClick'> {
  variant?: 'primary' | 'secondary' | 'danger' | 'ghost';
  size?: 'small' | 'medium' | 'large';
  loading?: boolean;
  disabled?: boolean;
  onClick?: (event: React.MouseEvent<HTMLButtonElement>) => void | Promise<void>;
}

// 입력 필드 Props
export interface InputProps extends BaseComponentProps, Omit<HTMLAttributes<HTMLInputElement>, 'onChange'> {
  type?: 'text' | 'email' | 'password' | 'number';
  placeholder?: string;
  value?: string;
  error?: string;
  required?: boolean;
  onChange?: (value: string) => void;
}

// 모달 Props
export interface ModalProps extends BaseComponentProps {
  isOpen: boolean;
  onClose: () => void;
  title?: string;
  size?: 'small' | 'medium' | 'large' | 'fullscreen';
}
```

#### 2.2 비즈니스 컴포넌트 Props
**파일**: `/workspace/vanillameta/frontend-web/src/components/User/UserCard.tsx`

```typescript
import React from 'react';
import { User } from '../../types/api';
import { BaseComponentProps } from '../../types/components';

export interface UserCardProps extends BaseComponentProps {
  user: User;
  onEdit?: (user: User) => void;
  onDelete?: (userId: number) => void;
  showActions?: boolean;
}

export const UserCard: React.FC<UserCardProps> = ({
  user,
  onEdit,
  onDelete,
  showActions = true,
  className,
  testId,
}) => {
  const handleEdit = (): void => {
    onEdit?.(user);
  };

  const handleDelete = (): void => {
    onDelete?.(user.id);
  };

  return (
    <div className={`user-card ${className || ''}`} data-testid={testId}>
      <div className="user-info">
        <h3>{user.name}</h3>
        <p>{user.email}</p>
        <span className={`status status-${user.status}`}>{user.status}</span>
      </div>
      
      {showActions && (
        <div className="user-actions">
          <button onClick={handleEdit}>Edit</button>
          <button onClick={handleDelete}>Delete</button>
        </div>
      )}
    </div>
  );
};
```

#### 2.3 폼 컴포넌트 타입 정의
**파일**: `/workspace/vanillameta/frontend-web/src/components/Forms/UserForm.tsx`

```typescript
import React from 'react';
import { useForm, SubmitHandler } from 'react-hook-form';
import { User } from '../../types/api';
import { BaseComponentProps } from '../../types/components';

// 폼 데이터 타입
export interface UserFormData {
  name: string;
  email: string;
  status: User['status'];
}

// 폼 Props
export interface UserFormProps extends BaseComponentProps {
  initialData?: Partial<UserFormData>;
  onSubmit: (data: UserFormData) => Promise<void>;
  loading?: boolean;
}

export const UserForm: React.FC<UserFormProps> = ({
  initialData,
  onSubmit,
  loading = false,
  className,
  testId,
}) => {
  const {
    register,
    handleSubmit,
    formState: { errors, isValid }
  } = useForm<UserFormData>({
    defaultValues: initialData,
    mode: 'onChange'
  });

  const onFormSubmit: SubmitHandler<UserFormData> = async (data) => {
    await onSubmit(data);
  };

  return (
    <form 
      onSubmit={handleSubmit(onFormSubmit)} 
      className={className}
      data-testid={testId}
    >
      {/* 폼 필드들 */}
    </form>
  );
};
```

### 3. 전역 상태 타입 정의

#### 3.1 Redux Toolkit 타입 설정
**파일**: `/workspace/vanillameta/frontend-web/src/store/types.ts`

```typescript
import { store } from './index';

// RootState 타입
export type RootState = ReturnType<typeof store.getState>;

// AppDispatch 타입
export type AppDispatch = typeof store.dispatch;

// Thunk Action 타입
export interface ThunkAPI {
  dispatch: AppDispatch;
  state: RootState;
  rejectValue: string;
}
```

#### 3.2 State Slice 타입 정의
**파일**: `/workspace/vanillameta/frontend-web/src/store/slices/userSlice.ts`

```typescript
import { createSlice, createAsyncThunk, PayloadAction } from '@reduxjs/toolkit';
import { User, PaginatedResponse } from '../../types/api';
import { ApiClient } from '../../api/client';

// User State 인터페이스
export interface UserState {
  users: User[];
  currentUser: User | null;
  pagination: {
    page: number;
    limit: number;
    total: number;
    totalPages: number;
  };
  loading: {
    list: boolean;
    detail: boolean;
    create: boolean;
    update: boolean;
    delete: boolean;
  };
  error: string | null;
}

// 초기 상태
const initialState: UserState = {
  users: [],
  currentUser: null,
  pagination: {
    page: 1,
    limit: 10,
    total: 0,
    totalPages: 0,
  },
  loading: {
    list: false,
    detail: false,
    create: false,
    update: false,
    delete: false,
  },
  error: null,
};

// Async Thunks
export const fetchUsers = createAsyncThunk<
  PaginatedResponse<User>,
  { page: number; limit: number },
  { rejectValue: string }
>('users/fetchUsers', async ({ page, limit }, { rejectWithValue }) => {
  try {
    const apiClient = new ApiClient(process.env.REACT_APP_API_URL!);
    const response = await apiClient.getUsers(page, limit);
    return response.data;
  } catch (error) {
    return rejectWithValue(error instanceof Error ? error.message : 'Unknown error');
  }
});

// Slice 정의
const userSlice = createSlice({
  name: 'users',
  initialState,
  reducers: {
    clearError: (state) => {
      state.error = null;
    },
    setCurrentUser: (state, action: PayloadAction<User | null>) => {
      state.currentUser = action.payload;
    },
  },
  extraReducers: (builder) => {
    builder
      .addCase(fetchUsers.pending, (state) => {
        state.loading.list = true;
        state.error = null;
      })
      .addCase(fetchUsers.fulfilled, (state, action) => {
        state.loading.list = false;
        state.users = action.payload.data;
        state.pagination = {
          page: action.payload.page,
          limit: action.payload.limit,
          total: action.payload.total,
          totalPages: action.payload.totalPages,
        };
      })
      .addCase(fetchUsers.rejected, (state, action) => {
        state.loading.list = false;
        state.error = action.payload || 'Failed to fetch users';
      });
  },
});

export const { clearError, setCurrentUser } = userSlice.actions;
export default userSlice.reducer;
```

#### 3.3 Custom Hooks 타입 정의
**파일**: `/workspace/vanillameta/frontend-web/src/hooks/useTypedSelector.ts`

```typescript
import { useSelector, TypedUseSelectorHook } from 'react-redux';
import { RootState } from '../store/types';

// 타입이 지정된 useSelector
export const useTypedSelector: TypedUseSelectorHook<RootState> = useSelector;
```

**파일**: `/workspace/vanillameta/frontend-web/src/hooks/useUsers.ts`

```typescript
import { useCallback, useEffect } from 'react';
import { useDispatch } from 'react-redux';
import { AppDispatch } from '../store/types';
import { useTypedSelector } from './useTypedSelector';
import { fetchUsers, clearError } from '../store/slices/userSlice';

export interface UseUsersReturn {
  users: User[];
  loading: boolean;
  error: string | null;
  pagination: {
    page: number;
    limit: number;
    total: number;
    totalPages: number;
  };
  refetch: (page?: number, limit?: number) => void;
  clearError: () => void;
}

export const useUsers = (): UseUsersReturn => {
  const dispatch = useDispatch<AppDispatch>();
  const { users, loading, error, pagination } = useTypedSelector((state) => state.users);

  const refetch = useCallback((page: number = 1, limit: number = 10) => {
    dispatch(fetchUsers({ page, limit }));
  }, [dispatch]);

  const handleClearError = useCallback(() => {
    dispatch(clearError());
  }, [dispatch]);

  useEffect(() => {
    refetch();
  }, [refetch]);

  return {
    users,
    loading: loading.list,
    error,
    pagination,
    refetch,
    clearError: handleClearError,
  };
};
```

### 4. 라우팅 타입 정의

#### 4.1 Route 파라미터 타입
**파일**: `/workspace/vanillameta/frontend-web/src/types/routes.ts`

```typescript
// 라우트 파라미터 타입들
export interface UserDetailParams {
  userId: string;
}

export interface DashboardParams {
  dashboardId: string;
}

export interface DataSourceParams {
  dataSourceId: string;
}

// 쿼리 파라미터 타입들
export interface UserListQuery {
  page?: string;
  limit?: string;
  search?: string;
  status?: string;
}

export interface DashboardListQuery {
  visibility?: string;
  sortBy?: string;
  sortOrder?: 'asc' | 'desc';
}
```

#### 4.2 Protected Route 타입
**파일**: `/workspace/vanillameta/frontend-web/src/components/ProtectedRoute.tsx`

```typescript
import React from 'react';
import { Navigate, useLocation } from 'react-router-dom';
import { useTypedSelector } from '../hooks/useTypedSelector';

export interface ProtectedRouteProps {
  children: React.ReactElement;
  requiredRole?: 'admin' | 'user';
  redirectTo?: string;
}

export const ProtectedRoute: React.FC<ProtectedRouteProps> = ({
  children,
  requiredRole,
  redirectTo = '/login',
}) => {
  const location = useLocation();
  const { user, isAuthenticated } = useTypedSelector((state) => state.auth);

  if (!isAuthenticated) {
    return <Navigate to={redirectTo} state={{ from: location }} replace />;
  }

  if (requiredRole && user?.role !== requiredRole) {
    return <Navigate to="/unauthorized" replace />;
  }

  return children;
};
```

### 5. 유틸리티 및 헬퍼 타입

#### 5.1 공통 유틸리티 타입
**파일**: `/workspace/vanillameta/frontend-web/src/types/utils.ts`

```typescript
// 공통 유틸리티 타입들
export type Optional<T, K extends keyof T> = Omit<T, K> & Partial<Pick<T, K>>;

export type RequiredKeys<T, K extends keyof T> = T & Required<Pick<T, K>>;

export type DeepPartial<T> = {
  [P in keyof T]?: T[P] extends object ? DeepPartial<T[P]> : T[P];
};

// 폼 관련 타입
export interface FormField<T> {
  value: T;
  error?: string;
  touched: boolean;
}

export interface FormState<T> {
  values: T;
  errors: Partial<Record<keyof T, string>>;
  touched: Partial<Record<keyof T, boolean>>;
  isValid: boolean;
  isSubmitting: boolean;
}

// API 관련 타입
export interface ApiError {
  message: string;
  code: string;
  details?: Record<string, any>;
}

export interface LoadingState {
  [key: string]: boolean;
}
```

#### 5.2 데이터 변환 헬퍼
**파일**: `/workspace/vanillameta/frontend-web/src/utils/typeHelpers.ts`

```typescript
import { User } from '../types/api';

// 날짜 문자열을 Date 객체로 변환
export const parseApiDate = (dateString: string): Date => {
  return new Date(dateString);
};

// API 응답 데이터 변환
export const transformUser = (apiUser: User): User => {
  return {
    ...apiUser,
    createdAt: parseApiDate(apiUser.createdAt).toISOString(),
    updatedAt: parseApiDate(apiUser.updatedAt).toISOString(),
  };
};

// 타입 가드 함수들
export const isUser = (obj: any): obj is User => {
  return obj && typeof obj.id === 'number' && typeof obj.name === 'string';
};

export const isApiError = (obj: any): obj is ApiError => {
  return obj && typeof obj.message === 'string' && typeof obj.code === 'string';
};
```

## 검증 기준

### 성공 조건
- [ ] 모든 React 컴포넌트에 완전한 Props 타입 정의
- [ ] API 응답 타입 자동 생성 및 적용
- [ ] Redux state 및 actions에 완전한 타입 정의
- [ ] Custom hooks에 정확한 반환 타입 정의
- [ ] any 타입 사용 완전 제거

### 품질 메트릭
- **타입 커버리지**: 컴포넌트/훅/스토어 계층 100%
- **컴파일 에러**: 0개
- **린트 에러**: 0개
- **번들 크기**: 기존 대비 5% 이내 증가

## 구현 순서

1. **API 타입 생성 도구 설정** (0.5일)
   - OpenAPI 도구 설정
   - API 타입 자동 생성
   - API 클라이언트 래퍼 구현

2. **컴포넌트 Props 타입 정의** (1일)
   - 기본 컴포넌트 타입 정의
   - 비즈니스 컴포넌트 타입 적용
   - 폼 컴포넌트 타입 정의

3. **상태 관리 타입 정의** (1일)
   - Redux store 타입 설정
   - Slice 타입 정의
   - Custom hooks 타입 정의

4. **유틸리티 및 헬퍼 타입** (0.5일)
   - 공통 타입 정의
   - 타입 가드 함수 구현
   - 라우팅 타입 정의

## 주요 파일 목록

### 수정 대상 파일들
- `/workspace/vanillameta/frontend-web/src/components/**/*.tsx`
- `/workspace/vanillameta/frontend-web/src/pages/**/*.tsx`
- `/workspace/vanillameta/frontend-web/src/hooks/**/*.ts`
- `/workspace/vanillameta/frontend-web/src/store/**/*.ts`

### 새로 생성할 파일들
- `/workspace/vanillameta/frontend-web/src/types/api.ts`
- `/workspace/vanillameta/frontend-web/src/types/components.ts`
- `/workspace/vanillameta/frontend-web/src/types/routes.ts`
- `/workspace/vanillameta/frontend-web/src/types/utils.ts`
- `/workspace/vanillameta/frontend-web/src/api/client.ts`
- `/workspace/vanillameta/frontend-web/src/utils/typeHelpers.ts`

## 주의사항

- **성능 최적화**: 타입 정의가 런타임 성능에 미치는 영향 최소화
- **개발자 경험**: 자동완성과 인텔리센스 향상에 집중
- **기존 코드 호환성**: 점진적 적용으로 기존 기능 영향 최소화
- **번들 크기**: 타입 정의가 최종 번들에 포함되지 않도록 주의

## 연관 태스크
- **T01**: TypeScript 설정 강화 (선행 필수)
- **T02**: 백엔드 타입 정의 개선 (API 타입 공유)
- **T04**: 공통 타입 라이브러리 구축 (타입 공유)
- **T05**: 타입 안전성 테스트 (프론트엔드 타입 검증)