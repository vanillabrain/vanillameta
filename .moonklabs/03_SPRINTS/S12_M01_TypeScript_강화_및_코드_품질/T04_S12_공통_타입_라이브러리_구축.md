# T04_S12: 공통 타입 라이브러리 구축

## 태스크 정보
- **스프린트**: S12 - TypeScript 강화 및 코드 품질
- **태스크 ID**: T04_S12
- **우선순위**: 중간
- **예상 소요시간**: 1-2일
- **담당자**: 풀스택 개발팀
- **의존성**: T02, T03 완료 후 진행

## 목표
백엔드와 프론트엔드 간에 공유되는 타입 정의를 중앙 집중화하여 일관성을 보장하고, 타입 불일치로 인한 버그를 방지합니다.

## 작업 내용

### 1. 공유 타입 정의 패키지 생성

#### 1.1 패키지 구조 설계
**새 디렉토리**: `/workspace/vanillameta/shared-types/`

```
shared-types/
├── package.json
├── tsconfig.json
├── src/
│   ├── index.ts
│   ├── entities/
│   │   ├── user.types.ts
│   │   ├── dashboard.types.ts
│   │   ├── data-source.types.ts
│   │   └── index.ts
│   ├── api/
│   │   ├── request.types.ts
│   │   ├── response.types.ts
│   │   └── index.ts
│   ├── common/
│   │   ├── pagination.types.ts
│   │   ├── validation.types.ts
│   │   ├── utility.types.ts
│   │   └── index.ts
│   └── enums/
│       ├── user-status.enum.ts
│       ├── dashboard-visibility.enum.ts
│       └── index.ts
├── dist/
└── README.md
```

#### 1.2 Package.json 설정
**파일**: `/workspace/vanillameta/shared-types/package.json`

```json
{
  "name": "@vanillameta/shared-types",
  "version": "1.0.0",
  "description": "Shared TypeScript types for VanillaMeta project",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist/**/*"
  ],
  "scripts": {
    "build": "tsc",
    "build:watch": "tsc --watch",
    "clean": "rimraf dist",
    "prepublishOnly": "npm run clean && npm run build"
  },
  "devDependencies": {
    "typescript": "^5.0.0",
    "rimraf": "^5.0.0"
  },
  "peerDependencies": {
    "typescript": ">=4.5.0"
  }
}
```

#### 1.3 TypeScript 설정
**파일**: `/workspace/vanillameta/shared-types/tsconfig.json`

```json
{
  "compilerOptions": {
    "target": "ES2020",
    "module": "CommonJS",
    "moduleResolution": "node",
    "declaration": true,
    "declarationMap": true,
    "outDir": "./dist",
    "rootDir": "./src",
    "strict": true,
    "esModuleInterop": true,
    "skipLibCheck": true,
    "forceConsistentCasingInFileNames": true,
    "noImplicitReturns": true,
    "noFallthroughCasesInSwitch": true,
    "noUncheckedIndexedAccess": true
  },
  "include": [
    "src/**/*"
  ],
  "exclude": [
    "node_modules",
    "dist"
  ]
}
```

### 2. 엔티티 타입 정의

#### 2.1 User 엔티티 타입
**파일**: `/workspace/vanillameta/shared-types/src/entities/user.types.ts`

```typescript
import { UserStatus } from '../enums/user-status.enum';
import { BaseEntity } from './base.types';

export interface User extends BaseEntity {
  id: number;
  name: string;
  email: string;
  status: UserStatus;
  role: UserRole;
  profileImage?: string;
  lastLoginAt?: Date;
}

export enum UserRole {
  ADMIN = 'admin',
  USER = 'user',
  VIEWER = 'viewer'
}

export interface UserProfile {
  userId: number;
  firstName?: string;
  lastName?: string;
  company?: string;
  department?: string;
  position?: string;
  phoneNumber?: string;
  timezone?: string;
  language?: string;
}

// User 관련 DTO 타입들
export interface CreateUserInput {
  name: string;
  email: string;
  role?: UserRole;
  status?: UserStatus;
}

export interface UpdateUserInput {
  name?: string;
  email?: string;
  role?: UserRole;
  status?: UserStatus;
  profileImage?: string;
}

export interface UserFilters {
  name?: string;
  email?: string;
  status?: UserStatus;
  role?: UserRole;
  createdAfter?: Date;
  createdBefore?: Date;
}
```

#### 2.2 Dashboard 엔티티 타입
**파일**: `/workspace/vanillameta/shared-types/src/entities/dashboard.types.ts`

```typescript
import { DashboardVisibility } from '../enums/dashboard-visibility.enum';
import { BaseEntity } from './base.types';

export interface Dashboard extends BaseEntity {
  id: number;
  title: string;
  description?: string;
  visibility: DashboardVisibility;
  userId: number;
  layout: DashboardLayout;
  settings: DashboardSettings;
  widgets: Widget[];
}

export interface DashboardLayout {
  columns: number;
  rowHeight: number;
  margin: [number, number];
  padding: [number, number];
}

export interface DashboardSettings {
  theme: 'light' | 'dark' | 'auto';
  refreshInterval?: number; // seconds
  allowExport: boolean;
  allowShare: boolean;
}

export interface Widget {
  id: string;
  type: WidgetType;
  title: string;
  position: WidgetPosition;
  size: WidgetSize;
  config: WidgetConfig;
  dataSourceId?: number;
  query?: string;
}

export enum WidgetType {
  CHART = 'chart',
  TABLE = 'table',
  METRIC = 'metric',
  TEXT = 'text',
  IMAGE = 'image'
}

export interface WidgetPosition {
  x: number;
  y: number;
}

export interface WidgetSize {
  width: number;
  height: number;
}

export interface WidgetConfig {
  chartType?: ChartType;
  showLegend?: boolean;
  showGrid?: boolean;
  colors?: string[];
  [key: string]: any;
}

export enum ChartType {
  LINE = 'line',
  BAR = 'bar',
  PIE = 'pie',
  DOUGHNUT = 'doughnut',
  AREA = 'area',
  SCATTER = 'scatter'
}

// Dashboard 관련 DTO 타입들
export interface CreateDashboardInput {
  title: string;
  description?: string;
  visibility: DashboardVisibility;
  layout?: Partial<DashboardLayout>;
  settings?: Partial<DashboardSettings>;
}

export interface UpdateDashboardInput {
  title?: string;
  description?: string;
  visibility?: DashboardVisibility;
  layout?: Partial<DashboardLayout>;
  settings?: Partial<DashboardSettings>;
}

export interface DashboardFilters {
  title?: string;
  visibility?: DashboardVisibility;
  userId?: number;
  createdAfter?: Date;
  createdBefore?: Date;
}
```

#### 2.3 Data Source 엔티티 타입
**파일**: `/workspace/vanillameta/shared-types/src/entities/data-source.types.ts`

```typescript
import { DataSourceType } from '../enums/data-source-type.enum';
import { BaseEntity } from './base.types';

export interface DataSource extends BaseEntity {
  id: number;
  name: string;
  type: DataSourceType;
  config: DataSourceConfig;
  userId: number;
  isActive: boolean;
  lastTestedAt?: Date;
  testResult?: DataSourceTestResult;
}

export interface DataSourceConfig {
  host?: string;
  port?: number;
  database?: string;
  username?: string;
  password?: string; // 암호화된 형태로 저장
  ssl?: boolean;
  connectionTimeout?: number;
  queryTimeout?: number;
  maxConnections?: number;
  
  // MongoDB 전용
  authDatabase?: string;
  replicaSet?: string;
  
  // DuckDB 전용
  filePath?: string;
  memoryLimit?: string;
  
  // 기타 설정
  [key: string]: any;
}

export interface DataSourceTestResult {
  success: boolean;
  message: string;
  responseTime?: number;
  testedAt: Date;
  version?: string;
  features?: string[];
}

// Data Source 관련 DTO 타입들
export interface CreateDataSourceInput {
  name: string;
  type: DataSourceType;
  config: DataSourceConfig;
}

export interface UpdateDataSourceInput {
  name?: string;
  config?: Partial<DataSourceConfig>;
  isActive?: boolean;
}

export interface TestDataSourceInput {
  type: DataSourceType;
  config: DataSourceConfig;
}

export interface DataSourceFilters {
  name?: string;
  type?: DataSourceType;
  isActive?: boolean;
  userId?: number;
}
```

#### 2.4 기본 엔티티 타입
**파일**: `/workspace/vanillameta/shared-types/src/entities/base.types.ts`

```typescript
export interface BaseEntity {
  createdAt: Date;
  updatedAt: Date;
}

export interface SoftDeleteEntity extends BaseEntity {
  deletedAt?: Date;
}

export interface UserOwnedEntity extends BaseEntity {
  userId: number;
}

export interface AuditableEntity extends BaseEntity {
  createdBy?: number;
  updatedBy?: number;
}
```

### 3. API 인터페이스 정의

#### 3.1 요청 타입 정의
**파일**: `/workspace/vanillameta/shared-types/src/api/request.types.ts`

```typescript
// 페이지네이션 요청
export interface PaginationRequest {
  page?: number;
  limit?: number;
  sortBy?: string;
  sortOrder?: 'ASC' | 'DESC';
}

// 검색 요청
export interface SearchRequest {
  query?: string;
  filters?: Record<string, any>;
}

// 파일 업로드 요청
export interface FileUploadRequest {
  file: File;
  category?: string;
  description?: string;
}

// 벌크 작업 요청
export interface BulkOperationRequest<T> {
  operation: 'create' | 'update' | 'delete';
  data: T[];
}

// 내보내기 요청
export interface ExportRequest {
  format: 'csv' | 'json' | 'xlsx';
  filters?: Record<string, any>;
  columns?: string[];
}
```

#### 3.2 응답 타입 정의
**파일**: `/workspace/vanillameta/shared-types/src/api/response.types.ts`

```typescript
// 기본 API 응답
export interface ApiResponse<T = any> {
  success: boolean;
  data: T;
  message?: string;
  timestamp: string;
}

// 에러 응답
export interface ApiErrorResponse {
  success: false;
  error: {
    code: string;
    message: string;
    details?: Record<string, any>;
    stack?: string; // 개발 환경에서만
  };
  timestamp: string;
}

// 페이지네이션 응답
export interface PaginatedResponse<T> {
  data: T[];
  pagination: {
    page: number;
    limit: number;
    total: number;
    totalPages: number;
    hasNext: boolean;
    hasPrev: boolean;
  };
}

// 파일 업로드 응답
export interface FileUploadResponse {
  filename: string;
  originalname: string;
  mimetype: string;
  size: number;
  path: string;
  url: string;
}

// 벌크 작업 응답
export interface BulkOperationResponse<T> {
  success: T[];
  failed: Array<{
    data: T;
    error: string;
  }>;
  total: number;
  successCount: number;
  failedCount: number;
}

// 통계 응답
export interface StatsResponse {
  [key: string]: number | string;
}

// 헬스체크 응답
export interface HealthCheckResponse {
  status: 'healthy' | 'degraded' | 'unhealthy';
  timestamp: string;
  uptime: number;
  version: string;
  services: Record<string, {
    status: 'up' | 'down';
    responseTime?: number;
    error?: string;
  }>;
}
```

### 4. 공통 유틸리티 타입

#### 4.1 유틸리티 타입 정의
**파일**: `/workspace/vanillameta/shared-types/src/common/utility.types.ts`

```typescript
// 선택적 필드 생성
export type Optional<T, K extends keyof T> = Omit<T, K> & Partial<Pick<T, K>>;

// 필수 필드 생성
export type Required<T, K extends keyof T> = T & Required<Pick<T, K>>;

// 깊은 부분 타입
export type DeepPartial<T> = {
  [P in keyof T]?: T[P] extends object ? DeepPartial<T[P]> : T[P];
};

// 깊은 필수 타입
export type DeepRequired<T> = {
  [P in keyof T]-?: T[P] extends object ? DeepRequired<T[P]> : T[P];
};

// 읽기 전용 깊은 타입
export type DeepReadonly<T> = {
  readonly [P in keyof T]: T[P] extends object ? DeepReadonly<T[P]> : T[P];
};

// 키 타입 추출
export type KeysOfType<T, U> = {
  [K in keyof T]: T[K] extends U ? K : never;
}[keyof T];

// 값 타입 추출
export type ValuesOf<T> = T[keyof T];

// 함수 파라미터 타입 추출
export type Parameters<T extends (...args: any) => any> = T extends (...args: infer P) => any ? P : never;

// 함수 반환 타입 추출
export type ReturnType<T extends (...args: any) => any> = T extends (...args: any) => infer R ? R : any;

// Null 및 undefined 제거
export type NonNullable<T> = T extends null | undefined ? never : T;

// 배열 요소 타입 추출
export type ArrayElement<T> = T extends readonly (infer U)[] ? U : never;

// Promise 해결 타입 추출
export type Awaited<T> = T extends Promise<infer U> ? U : T;
```

#### 4.2 검증 타입 정의
**파일**: `/workspace/vanillameta/shared-types/src/common/validation.types.ts`

```typescript
// 검증 규칙
export interface ValidationRule {
  required?: boolean;
  min?: number;
  max?: number;
  pattern?: RegExp;
  custom?: (value: any) => boolean | string;
}

// 필드 검증
export interface FieldValidation {
  value: any;
  rules: ValidationRule;
  error?: string;
}

// 폼 검증
export interface FormValidation<T> {
  fields: {
    [K in keyof T]: FieldValidation;
  };
  isValid: boolean;
  errors: Array<{
    field: keyof T;
    message: string;
  }>;
}

// 검증 결과
export interface ValidationResult {
  isValid: boolean;
  errors: ValidationError[];
}

export interface ValidationError {
  field: string;
  message: string;
  code: string;
}

// 스키마 검증
export interface Schema<T> {
  [K in keyof T]: ValidationRule;
}
```

### 5. Enum 정의

#### 5.1 사용자 상태 Enum
**파일**: `/workspace/vanillameta/shared-types/src/enums/user-status.enum.ts`

```typescript
export enum UserStatus {
  ACTIVE = 'active',
  INACTIVE = 'inactive',
  SUSPENDED = 'suspended',
  PENDING = 'pending'
}

export const USER_STATUS_LABELS: Record<UserStatus, string> = {
  [UserStatus.ACTIVE]: '활성',
  [UserStatus.INACTIVE]: '비활성',
  [UserStatus.SUSPENDED]: '일시정지',
  [UserStatus.PENDING]: '대기중'
};
```

#### 5.2 대시보드 가시성 Enum
**파일**: `/workspace/vanillameta/shared-types/src/enums/dashboard-visibility.enum.ts`

```typescript
export enum DashboardVisibility {
  PUBLIC = 'public',
  PRIVATE = 'private',
  SHARED = 'shared'
}

export const DASHBOARD_VISIBILITY_LABELS: Record<DashboardVisibility, string> = {
  [DashboardVisibility.PUBLIC]: '공개',
  [DashboardVisibility.PRIVATE]: '비공개',
  [DashboardVisibility.SHARED]: '공유'
};
```

#### 5.3 데이터 소스 타입 Enum
**파일**: `/workspace/vanillameta/shared-types/src/enums/data-source-type.enum.ts`

```typescript
export enum DataSourceType {
  MYSQL = 'mysql',
  POSTGRESQL = 'postgresql',
  MONGODB = 'mongodb',
  DUCKDB = 'duckdb',
  SQLITE = 'sqlite',
  MSSQL = 'mssql',
  ORACLE = 'oracle'
}

export const DATA_SOURCE_TYPE_LABELS: Record<DataSourceType, string> = {
  [DataSourceType.MYSQL]: 'MySQL',
  [DataSourceType.POSTGRESQL]: 'PostgreSQL',
  [DataSourceType.MONGODB]: 'MongoDB',
  [DataSourceType.DUCKDB]: 'DuckDB',
  [DataSourceType.SQLITE]: 'SQLite',
  [DataSourceType.MSSQL]: 'Microsoft SQL Server',
  [DataSourceType.ORACLE]: 'Oracle Database'
};
```

### 6. 인덱스 파일 및 내보내기

#### 6.1 메인 인덱스 파일
**파일**: `/workspace/vanillameta/shared-types/src/index.ts`

```typescript
// 엔티티 타입
export * from './entities';

// API 타입
export * from './api';

// 공통 타입
export * from './common';

// Enum 타입
export * from './enums';
```

#### 6.2 엔티티 인덱스 파일
**파일**: `/workspace/vanillameta/shared-types/src/entities/index.ts`

```typescript
export * from './base.types';
export * from './user.types';
export * from './dashboard.types';
export * from './data-source.types';
```

### 7. 패키지 통합 및 사용

#### 7.1 백엔드 통합
**파일**: `/workspace/vanillameta/backend-api/package.json`

dependencies에 추가:
```json
{
  "dependencies": {
    "@vanillameta/shared-types": "file:../shared-types"
  }
}
```

사용 예시:
```typescript
import { User, CreateUserInput, ApiResponse } from '@vanillameta/shared-types';

@Controller('users')
export class UserController {
  @Post()
  async createUser(@Body() userData: CreateUserInput): Promise<ApiResponse<User>> {
    // 구현
  }
}
```

#### 7.2 프론트엔드 통합
**파일**: `/workspace/vanillameta/frontend-web/package.json`

dependencies에 추가:
```json
{
  "dependencies": {
    "@vanillameta/shared-types": "file:../shared-types"
  }
}
```

사용 예시:
```typescript
import { User, Dashboard, ApiResponse } from '@vanillameta/shared-types';

interface UserCardProps {
  user: User;
  onEdit: (user: User) => void;
}

const UserCard: React.FC<UserCardProps> = ({ user, onEdit }) => {
  // 구현
};
```

## 검증 기준

### 성공 조건
- [ ] 공유 타입 패키지 성공적 빌드
- [ ] 백엔드에서 공유 타입 성공적 사용
- [ ] 프론트엔드에서 공유 타입 성공적 사용
- [ ] 타입 일관성 100% 보장
- [ ] 컴파일 에러 0개

### 품질 메트릭
- **타입 재사용률**: 80% 이상
- **타입 일관성**: 100%
- **빌드 성공률**: 100%
- **개발자 만족도**: 높음 (자동완성, 인텔리센스)

## 구현 순서

1. **패키지 구조 설정** (0.5일)
   - 패키지 디렉토리 생성
   - 빌드 설정 및 스크립트 구성

2. **핵심 타입 정의** (1일)
   - 엔티티 타입 정의
   - API 인터페이스 정의
   - Enum 정의

3. **유틸리티 타입 정의** (0.5일)
   - 공통 유틸리티 타입
   - 검증 타입 정의

4. **패키지 통합** (0.5일)
   - 백엔드 통합
   - 프론트엔드 통합
   - 테스트 및 검증

## 주의사항

- **버전 관리**: 타입 변경 시 semantic versioning 적용
- **하위 호환성**: 기존 타입과의 호환성 유지
- **문서화**: 타입 변경 시 changelog 업데이트
- **성능**: 타입 정의가 빌드 시간에 미치는 영향 최소화

## 연관 태스크
- **T02**: 백엔드 타입 정의 개선 (타입 공유를 위한 전제)
- **T03**: 프론트엔드 타입 정의 개선 (타입 공유를 위한 전제)  
- **T05**: 타입 안전성 테스트 (공유 타입 검증)
- **T06**: 코드 리뷰 및 문서화 (타입 사용 가이드라인)