# T02: 프론트엔드 테스트 환경 구축

## 태스크 정보
- **태스크 ID**: T02_S11
- **스프린트**: S11 - 테스트 인프라 긴급 복구
- **우선순위**: 긴급/높음
- **예상 소요 시간**: 1-2일
- **담당자**: Frontend Developer

## 개요

현재 프론트엔드에 적절한 테스트 환경이 구축되어 있지 않아 컴포넌트 및 기능 테스트가 불가능한 상태입니다. React Testing Library와 Jest를 활용한 안정적인 테스트 환경을 구축해야 합니다.

## 목표

1. React Testing Library 설정
2. Jest 설정 추가 및 최적화
3. 기본 컴포넌트 테스트 작성
4. 테스트 유틸리티 및 헬퍼 함수 구축

## 세부 작업 계획

### 1. 현재 상태 진단
- [ ] `frontend-web` 디렉토리의 테스트 관련 설정 확인
- [ ] `package.json`의 테스트 스크립트 및 의존성 확인
- [ ] Create React App 또는 Vite 설정 확인
- [ ] 기존 테스트 파일 존재 여부 점검

### 2. 테스트 라이브러리 설치 및 설정
- [ ] 필수 테스트 패키지 설치
  - `@testing-library/react`
  - `@testing-library/jest-dom`
  - `@testing-library/user-event`
  - `jest-environment-jsdom`
- [ ] TypeScript 타입 정의 패키지 설치
- [ ] MSW (Mock Service Worker) 설정 (API 모킹용)

### 3. Jest 설정 구성
- [ ] `jest.config.js` 또는 설정 파일 생성
- [ ] setupTests.js 파일 구성
- [ ] 모듈 경로 매핑 설정
- [ ] 정적 자원 모킹 설정 (CSS, 이미지 등)

### 4. 테스트 유틸리티 구축
- [ ] 공통 테스트 헬퍼 함수 작성
- [ ] Custom Render 함수 (Provider 포함)
- [ ] Mock 데이터 생성 유틸리티
- [ ] 테스트용 라우터 설정

### 5. 기본 컴포넌트 테스트 작성
- [ ] App 컴포넌트 smoke test
- [ ] 공통 UI 컴포넌트 테스트
- [ ] 페이지 컴포넌트 기본 테스트
- [ ] 유틸리티 함수 테스트

## 기술적 세부사항

### Jest 설정 예시
```javascript
// jest.config.js
module.exports = {
  testEnvironment: 'jsdom',
  setupFilesAfterEnv: ['<rootDir>/src/setupTests.ts'],
  moduleNameMapping: {
    '^@/(.*)$': '<rootDir>/src/$1',
    '\\.(css|less|scss|sass)$': 'identity-obj-proxy',
    '\\.(jpg|jpeg|png|gif|eot|otf|webp|svg|ttf|woff|woff2|mp4|webm|wav|mp3|m4a|aac|oga)$': '<rootDir>/src/__mocks__/fileMock.js'
  },
  collectCoverageFrom: [
    'src/**/*.{ts,tsx}',
    '!src/**/*.d.ts',
    '!src/index.tsx',
    '!src/reportWebVitals.ts'
  ],
  coverageReporters: ['text', 'lcov', 'html'],
  testMatch: [
    '<rootDir>/src/**/__tests__/**/*.{js,jsx,ts,tsx}',
    '<rootDir>/src/**/*.{spec,test}.{js,jsx,ts,tsx}'
  ],
  transform: {
    '^.+\\.(js|jsx|ts|tsx)$': 'babel-jest'
  }
};
```

### setupTests.ts 설정
```typescript
// src/setupTests.ts
import '@testing-library/jest-dom';
import { configure } from '@testing-library/react';

// React Testing Library 설정
configure({ testIdAttribute: 'data-testid' });

// MSW 설정 (필요시)
import { server } from './mocks/server';

beforeAll(() => server.listen());
afterEach(() => server.resetHandlers());
afterAll(() => server.close());

// 전역 모킹
Object.defineProperty(window, 'matchMedia', {
  writable: true,
  value: jest.fn().mockImplementation(query => ({
    matches: false,
    media: query,
    onchange: null,
    addListener: jest.fn(),
    removeListener: jest.fn(),
    addEventListener: jest.fn(),
    removeEventListener: jest.fn(),
    dispatchEvent: jest.fn(),
  })),
});
```

### 커스텀 렌더 함수
```typescript
// src/test-utils/render.tsx
import React, { ReactElement } from 'react';
import { render, RenderOptions } from '@testing-library/react';
import { BrowserRouter } from 'react-router-dom';
import { Provider } from 'react-redux';
import { store } from '../store';

interface CustomRenderOptions extends Omit<RenderOptions, 'wrapper'> {
  initialEntries?: string[];
}

const AllTheProviders = ({ children }: { children: React.ReactNode }) => {
  return (
    <Provider store={store}>
      <BrowserRouter>
        {children}
      </BrowserRouter>
    </Provider>
  );
};

const customRender = (
  ui: ReactElement,
  options: CustomRenderOptions = {}
) => {
  return render(ui, { wrapper: AllTheProviders, ...options });
};

export * from '@testing-library/react';
export { customRender as render };
```

### 패키지 의존성
```json
{
  "devDependencies": {
    "@testing-library/react": "^13.4.0",
    "@testing-library/jest-dom": "^5.16.5",
    "@testing-library/user-event": "^14.4.3",
    "@types/jest": "^29.5.0",
    "jest-environment-jsdom": "^29.5.0",
    "msw": "^1.2.0",
    "identity-obj-proxy": "^3.0.0"
  }
}
```

## 테스트 스크립트

### package.json 스크립트 추가
```json
{
  "scripts": {
    "test": "jest",
    "test:watch": "jest --watch",
    "test:coverage": "jest --coverage",
    "test:ci": "jest --coverage --watchAll=false",
    "test:debug": "jest --detectOpenHandles"
  }
}
```

## 기본 테스트 예시

### App 컴포넌트 테스트
```typescript
// src/App.test.tsx
import { render, screen } from './test-utils/render';
import App from './App';

describe('App', () => {
  test('renders without crashing', () => {
    render(<App />);
    expect(screen.getByRole('main')).toBeInTheDocument();
  });

  test('displays navigation menu', () => {
    render(<App />);
    expect(screen.getByRole('navigation')).toBeInTheDocument();
  });
});
```

### 컴포넌트 테스트 예시
```typescript
// src/components/Button/Button.test.tsx
import { render, screen, fireEvent } from '@testing-library/react';
import userEvent from '@testing-library/user-event';
import Button from './Button';

describe('Button', () => {
  test('renders button with text', () => {
    render(<Button>Click me</Button>);
    expect(screen.getByRole('button', { name: 'Click me' })).toBeInTheDocument();
  });

  test('calls onClick handler when clicked', async () => {
    const handleClick = jest.fn();
    const user = userEvent.setup();
    
    render(<Button onClick={handleClick}>Click me</Button>);
    
    await user.click(screen.getByRole('button'));
    expect(handleClick).toHaveBeenCalledTimes(1);
  });

  test('is disabled when disabled prop is true', () => {
    render(<Button disabled>Click me</Button>);
    expect(screen.getByRole('button')).toBeDisabled();
  });
});
```

## MSW 설정 (API 모킹)

### Mock 서버 설정
```typescript
// src/mocks/server.ts
import { setupServer } from 'msw/node';
import { handlers } from './handlers';

export const server = setupServer(...handlers);
```

### Mock 핸들러 예시
```typescript
// src/mocks/handlers.ts
import { rest } from 'msw';

export const handlers = [
  rest.get('/api/dashboard', (req, res, ctx) => {
    return res(
      ctx.status(200),
      ctx.json({
        id: 1,
        title: 'Test Dashboard',
        widgets: []
      })
    );
  }),

  rest.post('/api/auth/login', (req, res, ctx) => {
    return res(
      ctx.status(200),
      ctx.json({
        token: 'mock-token',
        user: { id: 1, email: 'test@example.com' }
      })
    );
  })
];
```

## 검증 방법

1. **기본 테스트 실행**
   ```bash
   cd frontend-web
   npm test
   ```

2. **커버리지 확인**
   ```bash
   npm run test:coverage
   ```

3. **Watch 모드 확인**
   ```bash
   npm run test:watch
   ```

## 성공 기준

- [ ] `npm test` 명령이 오류 없이 실행됨
- [ ] 기본 컴포넌트 테스트가 통과함
- [ ] 테스트 커버리지 리포트가 생성됨
- [ ] MSW를 통한 API 모킹이 정상 작동함
- [ ] CI/CD 파이프라인에서 테스트가 실행됨

## 리스크 및 대응 방안

### React 버전 호환성
- **리스크**: React 버전과 Testing Library 버전 불일치
- **대응**: 호환되는 버전으로 설치하거나 업그레이드

### CSS-in-JS 라이브러리 모킹
- **리스크**: styled-components 등의 CSS-in-JS 라이브러리 테스트 문제
- **대응**: 적절한 모킹 설정 및 snapshot 테스트 활용

### 비동기 컴포넌트 테스트
- **리스크**: API 호출이 포함된 컴포넌트 테스트 복잡성
- **대응**: MSW를 활용한 API 모킹 및 적절한 대기 로직

## 관련 파일

### 생성할 파일
- `frontend-web/jest.config.js`
- `frontend-web/src/setupTests.ts`
- `frontend-web/src/test-utils/render.tsx`
- `frontend-web/src/mocks/server.ts`
- `frontend-web/src/mocks/handlers.ts`
- `frontend-web/src/__mocks__/fileMock.js`

### 수정할 파일
- `frontend-web/package.json` (스크립트 및 의존성)

### 테스트 파일 생성
- `frontend-web/src/App.test.tsx`
- `frontend-web/src/components/**/*.test.tsx`

## 다음 태스크 연결

이 태스크 완료 후:
- T03: Mock 및 Fixture 정리에서 프론트엔드 Mock 데이터 구조화
- T05: 핵심 모듈 테스트 작성에서 Dashboard 및 Widget 컴포넌트 테스트 구현